# try to create an empty set, check using the type() function weather the type of variable is a set.
Anum = set()
print(type(Anum))
if("check using the type(), if variable"):
    print("The empty set is a variable")
else:
    print("The empty set is not a variable")